spring:
  profiles:
    active: test
  application:
    name: typescat-breeds
  cloud:
    consul:
      enabled: false
      host: 0.0.0.0:8500
      config:
        format: YAML
        watch:
          delay: 30000
      discovery:
        register-health-check: false
    loadbalancer:
      ribbon:
        enabled: false


securitycat: bigsecurity